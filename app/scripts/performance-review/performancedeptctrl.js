define(['./module', './performancedeptservice'],function(performance){
    'use strict';
    performance.controller('performanceDeptCtrl', [
        '$scope','$timeout','performanceDeptService',
        function($scope, $timeout,oaService, performanceDeptService){
        }
    ]);
});
